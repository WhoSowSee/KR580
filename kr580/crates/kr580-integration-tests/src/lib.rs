//! Workspace-level integration tests live in `tests/`. The library is
//! intentionally empty: the `tests/` directory holds Cargo integration tests
//! that exercise the public APIs of the other crates.
