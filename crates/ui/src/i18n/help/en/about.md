KR580 is an emulator for a microprocessor system based on the Intel 8080-compatible KR580VM80A.

The application displays registers, flags, RAM, machine cycles, and processor T-states. You can enter programs as hexadecimal bytes, execute them by T-state or instruction, use ports 00h–04h, and save results to files.
