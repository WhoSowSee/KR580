Export Data (Ctrl+E) saves a selected range of RAM cells to a separate file.

- Select address range (start and end)
- Save dialog
- Can export code fragments (for later import) or data
- Exported file can be loaded via Import (Ctrl+I) in another session

Useful for:
- Extracting subroutines from large projects
- Saving data tables
- Sharing code between different snapshots