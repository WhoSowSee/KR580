Export (Ctrl+E) creates TXT or XLSX from the current processor state.

Each XLSX page or TXT section can select its own RAM range, address/value/instruction columns, registers, and flags. XLSX pages become sheets in one workbook; TXT sections become named blocks in one file.

Export does not change the session. The result can later be imported as a whole, by sheet, or by section.
